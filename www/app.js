/* VidyaDesk v2 application shell — aligned with supabase_schema.sql */
const {createClient}=window.supabase;
const CFG={url:localStorage.getItem('vd_url')||'',key:localStorage.getItem('vd_key')||''};
let db=CFG.url&&CFG.key?createClient(CFG.url,CFG.key,{auth:{persistSession:true,autoRefreshToken:true}}):null;
const S={session:null,profile:null,institute:null,page:'dashboard',role:null,cache:{}};
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const money=n=>'₹'+Number(n||0).toLocaleString('en-IN');
const $=id=>document.getElementById(id);
function toast(m){const n=document.createElement('div');n.className='toast';n.textContent=m;document.body.appendChild(n);setTimeout(()=>n.remove(),2500)}
async function rows(table,filter={}){if(!db)return[];let q=db.from(table).select('*');for(const[k,v]of Object.entries(filter))q=q.eq(k,v);const{data,error}=await q; if(error)throw error;return data||[]}
async function add(table,row){const{data,error}=await db.from(table).insert(row).select().single();if(error)throw error;return data}
async function edit(table,id,row){const{data,error}=await db.from(table).update(row).eq('id',id).select().single();if(error)throw error;return data}
function modal(html){let m=$('modal');if(!m){m=document.createElement('div');m.id='modal';document.body.appendChild(m)}m.innerHTML='<div class="modalbox">'+html+'</div>';m.className='modal open'}
function closeModal(){const m=$('modal');if(m)m.className='modal'}
const menus={admin:[['dashboard','Dashboard'],['students','Students'],['families','Families'],['batches','Batches & Fee Plans'],['fees','Fee Collection'],['accounts','Accounts & Expenses'],['attendance','Attendance'],['staff','Staff & Payroll'],['exams','Exams & Results'],['learning','Courses & Material'],['leads','Leads / Enquiries'],['reports','Reports'],['access','Online Access'],['settings','Settings']],teacher:[['dashboard','Dashboard'],['batches','My Batches'],['attendance','Attendance'],['homework','Homework'],['learning','Courses & Material'],['exams','Exams & Marks'],['reports','Reports']],student:[['dashboard','Dashboard'],['fees','My Fees'],['attendance','My Attendance'],['exams','Results & Exams'],['learning','Study Material'],['timetable','Timetable'],['profile','Profile']],parent:[['dashboard','Dashboard'],['fees','Child Fees'],['attendance','Child Attendance'],['exams','Results'],['learning','Study Material']],employee:[['dashboard','Dashboard'],['attendance','My Attendance'],['staff','Payroll & Leave'],['profile','Profile']]};
function shell(content){const ms=menus[S.role]||menus.student;return `<div class="top"><button onclick="drawer()">☰</button><b>VidyaDesk</b><span></span><button onclick="location.reload()">↻</button><button onclick="logout()">↪</button></div><div class="layout"><aside><div class="brand"><strong>V</strong><div><b>VidyaDesk</b><small>${esc(S.institute?.name||'Education Management')}</small></div></div>${ms.map(x=>`<button class="nav ${S.page===x[0]?'on':''}" onclick="go('${x[0]}')">${x[1]}</button>`).join('')}</aside><main>${content}</main></div>`}
function go(p){S.page=p;render()}
function drawer(){document.querySelector('aside')?.classList.toggle('show')}
function css(){return `<style>:root{--p:#4338ca;--p2:#4f46e5;--bg:#f7f7fb;--card:#fff;--line:#e4e4ef;--text:#202033;--muted:#70708a}*{box-sizing:border-box}body{margin:0;font-family:Inter,system-ui,Arial;background:var(--bg);color:var(--text)}button,input,select,textarea{font:inherit}button{cursor:pointer}.top{height:58px;background:var(--p);color:white;display:flex;align-items:center;gap:12px;padding:0 14px;position:sticky;top:0;z-index:10}.top span{flex:1}.top button{background:none;border:0;color:#fff;font-size:20px}.layout{display:flex;min-height:calc(100vh - 58px)}aside{width:250px;background:#fff;border-right:1px solid var(--line);padding:12px;position:sticky;top:58px;height:calc(100vh - 58px);overflow:auto}.brand{display:flex;gap:10px;align-items:center;padding:10px;border-bottom:1px solid var(--line);margin-bottom:10px}.brand strong{width:42px;height:42px;background:var(--p);color:#fff;border-radius:12px;display:grid;place-items:center;font-size:22px}.brand small{display:block;color:var(--muted);font-size:11px}.nav{width:100%;padding:10px;border:0;background:transparent;text-align:left;border-radius:9px;margin:2px 0;color:#4b4b68}.nav.on,.nav:hover{background:#eef0ff;color:var(--p);font-weight:700}main{flex:1;padding:16px;max-width:1400px}.hero{background:linear-gradient(135deg,var(--p2),var(--p));color:#fff;border-radius:18px;padding:20px}.hero h1{margin:5px 0}.grid{display:grid;gap:12px}.g4{grid-template-columns:repeat(4,1fr)}.g3{grid-template-columns:repeat(3,1fr)}.g2{grid-template-columns:repeat(2,1fr)}.card{background:var(--card);border:1px solid var(--line);border-radius:14px;padding:14px}.metric{font-size:25px;font-weight:800;margin-top:6px}.muted{color:var(--muted)}.title{display:flex;align-items:center;gap:10px;margin:8px 0 12px}.title h2{margin:0;font-size:20px}.spacer{flex:1}.btn{border:0;border-radius:9px;padding:9px 13px;background:var(--p);color:#fff;font-weight:700}.btn.alt{background:#fff;color:var(--p);border:1px solid #cfcff0}.btn.good{background:#16834f}.btn.warn{background:#d97706}.field{display:grid;gap:5px}.field label{font-size:12px;color:var(--muted)}input,select,textarea{width:100%;padding:10px;border:1px solid #d7d7e7;border-radius:9px;background:#fff}.form{display:grid;grid-template-columns:1fr 1fr;gap:12px}.full{grid-column:1/-1}.tablewrap{overflow:auto;background:#fff;border:1px solid var(--line);border-radius:12px}.table{width:100%;border-collapse:collapse;min-width:700px}.table th,.table td{padding:10px;border-bottom:1px solid var(--line);text-align:left;font-size:13px}.table th{background:#f5f5ff}.badge{display:inline-block;border-radius:999px;padding:4px 8px;font-size:11px;font-weight:700;background:#eee}.active,.paid{background:#e6f6ed;color:#08653a}.pending{background:#fff0d0;color:#8a5a00}.quick{display:grid;grid-template-columns:repeat(4,1fr);gap:10px}.quick button{padding:15px;background:#fff;border:1px solid var(--line);border-radius:12px}.modal{position:fixed;inset:0;background:#0006;display:none;align-items:center;justify-content:center;padding:15px;z-index:30}.modal.open{display:flex}.modalbox{background:#fff;border-radius:16px;padding:16px;width:min(760px,100%);max-height:90vh;overflow:auto}.toast{position:fixed;right:15px;bottom:15px;background:#222;color:#fff;padding:10px 14px;border-radius:10px;z-index:99}.auth{min-height:100vh;display:grid;place-items:center;background:linear-gradient(160deg,var(--p),#eef0ff);padding:15px}.authbox{width:min(440px,100%);background:#fff;border-radius:18px;padding:22px}.logo{text-align:center;color:#fff;margin-bottom:18px}.logo b{display:block;background:#fff;color:var(--p);width:68px;height:68px;border-radius:18px;display:grid;place-items:center;margin:auto;font-size:28px}.tabs{display:grid;grid-template-columns:repeat(5,1fr);gap:5px}.tabs button{padding:8px;border:0;border-radius:8px;background:#f1f1ff}.tabs .sel{background:var(--p);color:#fff}.empty{text-align:center;padding:30px;color:var(--muted)}@media(max-width:850px){aside{display:none;position:fixed;z-index:20;left:0;top:58px;width:260px;height:calc(100vh - 58px)}aside.show{display:block}.g4,.g3{grid-template-columns:1fr 1fr}}@media(max-width:560px){main{padding:10px}.g4,.g3,.g2,.form{grid-template-columns:1fr}.quick{grid-template-columns:1fr 1fr}.tabs{grid-template-columns:1fr 1fr 1fr 1fr 1fr}.tabs button{font-size:11px}}</style>`}
function auth(){document.body.innerHTML=css()+`<div class="auth"><div class="authbox"><div class="logo"><b>V</b><h2>VidyaDesk</h2><div>Complete Institute Management</div></div><div class="tabs">${['admin','teacher','student','parent','employee'].map(r=>`<button class="${S.role===r?'sel':''}" onclick="S.role='${r}';auth()">${r}</button>`).join('')}</div><div class="form" style="margin-top:14px"><div class="field full"><label>Email</label><input id="email" type="email" placeholder="name@example.com"></div><div class="field full"><label>Password</label><input id="pass" type="password"></div></div><button class="btn" style="width:100%;margin-top:12px" onclick="login()">Login</button><button class="btn alt" style="width:100%;margin-top:8px" onclick="register()">Create Owner Account</button><div class="muted" style="font-size:11px;margin-top:12px">Supabase URL/key can be configured in browser storage. Email/SMTP settings are never changed by this app.</div></div></div>`}
async function login(){try{if(!db)throw Error('Configure Supabase first');const{data,error}=await db.auth.signInWithPassword({email:$('email').value.trim(),password:$('pass').value});if(error)throw error;S.session=data.session;await hydrate();toast('Welcome to VidyaDesk');render()}catch(e){toast(e.message)}}
async function register(){try{if(!db)throw Error('Configure Supabase first');const email=$('email').value.trim(),pass=$('pass').value;if(!email||!pass)throw Error('Enter email and password');const{data,error}=await db.auth.signUp({email,password:pass,options:{data:{full_name:email.split('@')[0]}}});if(error)throw error;toast(data.session?'Account created':'Confirm your email, then login')}catch(e){toast(e.message)}}
async function logout(){if(db)await db.auth.signOut();S.session=null;S.profile=null;auth()}
async function hydrate(){const{data:p,error}=await db.from('profiles').select('*').eq('id',S.session.user.id).maybeSingle();if(error)throw error;S.profile=p;if(!p){throw Error('Profile missing. Run the fresh schema and create the owner profile.')}S.role=p.role; if(p.institute_id){const{data:i}=await db.from('institutes').select('*').eq('id',p.institute_id).single();S.institute=i}}
function setup(){document.body.innerHTML=css()+`<div class="auth"><div class="authbox"><div class="logo"><b>V</b><h2>VidyaDesk</h2></div><h2>Institute Setup</h2><div class="form"><div class="field full"><label>Institute Name</label><input id="in" placeholder="Your coaching institute"></div><div class="field"><label>Phone</label><input id="phone"></div><div class="field"><label>Institute Type</label><select id="type"><option value="school_tuition">School Tuition</option><option value="competitive_exam">Competitive Exam</option><option value="hybrid">Hybrid</option></select></div><div class="field full"><label>Address</label><textarea id="addr"></textarea></div><div class="field"><label>City</label><input id="city"></div><div class="field"><label>State</label><input id="state"></div><div class="field"><label>GST Registered?</label><select id="gst"><option value="false">No</option><option value="true">Yes</option></select></div><div class="field"><label>GSTIN (only if enabled)</label><input id="gstin"></div></div><button class="btn" style="margin-top:12px" onclick="saveSetup()">Activate Institute</button></div></div>`}
async function saveSetup(){try{const i=await add('institutes',{name:$('in').value.trim(),phone:$('phone').value.trim(),address:$('addr').value.trim(),city:$('city').value.trim(),state:$('state').value.trim(),institute_type:$('type').value,gst_enabled:$('gst').value==='true',gstin:$('gst').value==='true'?$('gstin').value.trim():null});await edit('profiles',S.profile.id,{institute_id:i.id,role:'owner'});await hydrate();render()}catch(e){toast(e.message)}}
async function load(table,filter){S.cache[table]=await rows(table,filter||{institute_id:S.profile.institute_id});return S.cache[table]}
async function render(){if(!S.session||!S.profile){auth();return}if(!S.profile.institute_id){setup();return}document.body.innerHTML=css()+shell(await page())}
async function page(){switch(S.page){case'dashboard':return dashboard();case'students':return crudStudents();case'families':return generic('families','Families');case'batches':return batches();case'fees':return fees();case'accounts':return accounts();case'attendance':return attendance();case'staff':return staff();case'exams':return exams();case'learning':return learning();case'leads':return leads();case'reports':return reports();case'access':return access();case'timetable':return timetable();case'profile':return profile();case'settings':return settings();default:return dashboard()}}
async function dashboard(){if(S.role==='student'||S.role==='parent')return `<div class="hero"><div>VidyaDesk</div><h1>Welcome, ${esc(S.profile.full_name)}</h1><div>Learning, fees, attendance and results in one place.</div></div><div class="grid g2" style="margin-top:12px"><div class="card"><h3>My Learning</h3><button class="btn" onclick="go('learning')">Open Study Material</button></div><div class="card"><h3>My Results</h3><button class="btn alt" onclick="go('exams')">Open Results</button></div></div>`;const students=await load('students'),batches=await load('batches'),bills=await load('fee_bills'),leads=await load('leads');const due=bills.reduce((n,x)=>n+Number(x.net_amount||0),0);return `<div class="hero"><div>Admin Workspace</div><h1>Welcome to VidyaDesk</h1><div>Minimal-tap institute operations for India.</div></div><div class="grid g4" style="margin-top:12px"><div class="card"><div class="muted">Students</div><div class="metric">${students.filter(x=>x.status==='active').length}</div></div><div class="card"><div class="muted">Batches</div><div class="metric">${batches.length}</div></div><div class="card"><div class="muted">Outstanding Bills</div><div class="metric">${money(due)}</div></div><div class="card"><div class="muted">Open Leads</div><div class="metric">${leads.filter(x=>!['converted','lost','closed'].includes(x.status)).length}</div></div></div><div class="title"><h2>Quick Actions</h2></div><div class="quick">${[['students','👨‍🎓','Add Student'],['fees','₹','Collect Fees'],['attendance','✓','Attendance'],['exams','☑','Create Exam'],['learning','📚','Study Material'],['leads','📞','New Enquiry'],['staff','👥','Payroll'],['reports','▤','Reports']].map(x=>`<button onclick="go('${x[0]}')"><div style="font-size:22px">${x[1]}</div>${x[2]}</button>`).join('')}</div>`}
async function crudStudents(){const a=await load('students');return `<div class="title"><h2>Students</h2><span class="spacer"></span><button class="btn" onclick="studentForm()">+ Student</button></div><div class="toolbar"><input id="search" placeholder="Search name, guardian or mobile" oninput="filter()"></div><div class="tablewrap"><table class="table"><thead><tr><th>Name</th><th>Guardian</th><th>Mobile</th><th>School/Class</th><th>Status</th><th></th></tr></thead><tbody>${a.map(s=>`<tr><td><b>${esc(s.full_name)}</b><div class="muted">${esc(s.student_code||'')}</div></td><td>${esc(s.guardian_name||s.father_name||'')}</td><td>${esc(s.mobile||'')}</td><td>${esc(s.school||'')} ${esc(s.class_name||'')}</td><td><span class="badge ${s.status==='active'?'active':'pending'}">${esc(s.status)}</span></td><td><button class="btn alt" onclick='studentForm(${JSON.stringify(s)})'>Edit</button></td></tr>`).join('')}</tbody></table></div>`}
function studentForm(s={}){modal(`<div class="title"><h2>${s.id?'Edit':'Add'} Student</h2><span class="spacer"></span><button class="btn alt" onclick="closeModal()">Close</button></div><div class="form"><div class="field"><label>Full Name *</label><input id="sn" value="${esc(s.full_name)}"></div><div class="field"><label>Student Code</label><input id="sc" value="${esc(s.student_code||'STU'+Date.now().toString().slice(-6))}"></div><div class="field"><label>Father Name</label><input id="sf" value="${esc(s.father_name)}"></div><div class="field"><label>Guardian Mobile</label><input id="sm" value="${esc(s.mobile)}"></div><div class="field"><label>School</label><input id="ss" value="${esc(s.school)}"></div><div class="field"><label>Class</label><input id="cl" value="${esc(s.class_name)}"></div><div class="field"><label>Join Date</label><input id="sj" type="date" value="${esc(s.join_date||new Date().toISOString().slice(0,10))}"></div><div class="field"><label>Status</label><select id="st"><option ${s.status==='active'?'selected':''}>active</option><option ${s.status==='inactive'?'selected':''}>inactive</option><option ${s.status==='completed'?'selected':''}>completed</option><option ${s.status==='left'?'selected':''}>left</option></select></div><div class="field full"><label>Address</label><textarea id="sa">${esc(s.address)}</textarea></div></div><button class="btn" style="margin-top:12px" onclick="saveStudent('${s.id||''}')">Save Student</button>`)}
async function saveStudent(id){try{const r={institute_id:S.profile.institute_id,full_name:$('sn').value.trim(),student_code:$('sc').value.trim(),father_name:$('sf').value.trim(),mobile:$('sm').value.trim(),school:$('ss').value.trim(),class_name:$('cl').value.trim(),join_date:$('sj').value,status:$('st').value,address:$('sa').value.trim()};if(id)await edit('students',id,r);else await add('students',r);closeModal();toast('Student saved');render()}catch(e){toast(e.message)}}
function filter(){const v=$('search').value.toLowerCase();document.querySelectorAll('tbody tr').forEach(r=>r.style.display=r.innerText.toLowerCase().includes(v)?'':'none')}
async function batches(){const a=await load('batches');return `<div class="title"><h2>Batches & Fee Plans</h2><span class="spacer"></span><button class="btn" onclick="batchForm()">+ Batch</button></div><div class="grid g3">${a.map(b=>`<div class="card"><h3>${esc(b.name)}</h3><div class="muted">${esc(b.class_name||'')} ${esc(b.subject||'')}</div><p>${esc(b.course_name||'')}</p><span class="badge active">${esc(b.status)}</span></div>`).join('')||'<div class="empty">No batches yet.</div>'}</div>`}
function batchForm(){modal(`<div class="title"><h2>New Batch</h2><span class="spacer"></span><button class="btn alt" onclick="closeModal()">Close</button></div><div class="form"><div class="field"><label>Batch Name</label><input id="bn"></div><div class="field"><label>Style</label><select id="bs"><option value="class_wise">Class-wise</option><option value="subject_wise">Subject-wise</option><option value="competitive">Competitive Course</option></select></div><div class="field"><label>Class</label><input id="bc"></div><div class="field"><label>Subject</label><input id="bsub"></div><div class="field"><label>Course</label><input id="bco"></div><div class="field"><label>Duration</label><input id="bd"></div></div><button class="btn" style="margin-top:12px" onclick="saveBatch()">Save Batch</button>`)}
async function saveBatch(){try{await add('batches',{institute_id:S.profile.institute_id,name:$('bn').value.trim(),organization_style:$('bs').value,class_name:$('bc').value.trim(),subject:$('bsub').value.trim(),course_name:$('bco').value.trim(),duration:$('bd').value.trim()});closeModal();render()}catch(e){toast(e.message)}}
async function fees(){const a=await load('fee_bills');const p=await load('fee_payments');const total=a.reduce((n,x)=>n+Number(x.net_amount||0),0),paid=p.reduce((n,x)=>n+Number(x.amount||0),0);return `<div class="title"><h2>Fee Collection</h2><span class="spacer"></span><button class="btn" onclick="feeForm()">+ Collect</button></div><div class="grid g3"><div class="card"><div class="muted">Billed</div><div class="metric">${money(total)}</div></div><div class="card"><div class="muted">Collected</div><div class="metric">${money(paid)}</div></div><div class="card"><div class="muted">Outstanding</div><div class="metric">${money(Math.max(total-paid,0))}</div></div></div><div class="card" style="margin-top:12px"><h3>Recent Bills</h3><div class="tablewrap"><table class="table"><tr><th>Bill</th><th>Description</th><th>Due</th><th>Status</th></tr>${a.slice(-20).reverse().map(x=>`<tr><td>${esc(x.bill_number)}</td><td>${esc(x.description)}</td><td>${money(x.net_amount)}</td><td><span class="badge ${x.status==='paid'?'paid':'pending'}">${esc(x.status)}</span></td></tr>`).join('')}</table></div></div>`}
function feeForm(){modal(`<div class="title"><h2>Collect Payment</h2><span class="spacer"></span><button class="btn alt" onclick="closeModal()">Close</button></div><div class="form"><div class="field full"><label>Bill ID</label><input id="fb" placeholder="Paste fee bill UUID"></div><div class="field"><label>Amount</label><input id="fa" type="number"></div><div class="field"><label>Mode</label><select id="fm"><option value="cash">Cash</option><option value="online">Online</option><option value="upi">UPI</option></select></div><div class="field full"><label>Reference</label><input id="fr"></div></div><button class="btn" style="margin-top:12px" onclick="savePayment()">Record Payment</button>`)}
async function savePayment(){try{await add('fee_payments',{institute_id:S.profile.institute_id,bill_id:$('fb').value.trim(),amount:Number($('fa').value),payment_mode:$('fm').value,reference_no:$('fr').value.trim(),received_by:S.profile.id});closeModal();toast('Payment recorded');render()}catch(e){toast(e.message)}}
async function attendance(){const a=await load('attendance');return `<div class="title"><h2>Attendance</h2><span class="spacer"></span><button class="btn" onclick="attForm()">Mark Attendance</button></div><div class="card"><div class="metric">${a.filter(x=>x.status==='present').length}</div><div class="muted">Present records loaded</div></div><div class="tablewrap" style="margin-top:12px"><table class="table"><tr><th>Date</th><th>Student</th><th>Status</th><th>Source</th></tr>${a.slice(-30).reverse().map(x=>`<tr><td>${esc(x.attendance_date)}</td><td>${esc(x.student_id)}</td><td>${esc(x.status)}</td><td>${esc(x.source)}</td></tr>`).join('')}</table></div>`}
function attForm(){modal(`<div class="title"><h2>Quick Attendance</h2><span class="spacer"></span><button class="btn alt" onclick="closeModal()">Close</button></div><div class="form"><div class="field"><label>Student UUID</label><input id="as"></div><div class="field"><label>Batch UUID</label><input id="ab"></div><div class="field"><label>Date</label><input id="ad" type="date" value="${new Date().toISOString().slice(0,10)}"></div><div class="field"><label>Status</label><select id="at"><option>present</option><option>absent</option><option>late</option><option>leave</option></select></div></div><button class="btn" style="margin-top:12px" onclick="saveAtt()">Save</button>`)}
async function saveAtt(){try{await add('attendance',{institute_id:S.profile.institute_id,student_id:$('as').value,batch_id:$('ab').value||null,attendance_date:$('ad').value,status:$('at').value,marked_by:S.profile.id,source:'manual'});closeModal();toast('Attendance saved');render()}catch(e){toast(e.message)}}
async function staff(){const[a,b,c]=await Promise.all([load('employees'),load('teachers'),load('payroll')]);return `<div class="title"><h2>Staff & Payroll</h2></div><div class="grid g3"><div class="card"><h3>Employees</h3><div class="metric">${a.length}</div></div><div class="card"><h3>Teachers</h3><div class="metric">${b.length}</div></div><div class="card"><h3>Payroll Records</h3><div class="metric">${c.length}</div></div></div><div class="card" style="margin-top:12px"><h3>Leave & Salary</h3><div class="empty">Employee master, salary payments, payslips and leave approvals use the new payroll/leave tables.</div></div>`}
async function exams(){const a=await load('exams'),m=await load('exam_marks'),r=await load('results');return `<div class="title"><h2>Exams & Results</h2><span class="spacer"></span><button class="btn" onclick="examForm()">+ Exam</button></div><div class="grid g3">${a.map(x=>`<div class="card"><h3>${esc(x.title)}</h3><div class="muted">${esc(x.status)} • ${esc(x.duration_minutes)} min</div></div>`).join('')||'<div class="empty">No exams.</div>'}</div><div class="grid g2" style="margin-top:12px"><div class="card"><b>Marks entered</b><div class="metric">${m.length}</div></div><div class="card"><b>Result cards</b><div class="metric">${r.length}</div></div></div>`}
function examForm(){modal(`<div class="title"><h2>Create Exam</h2><span class="spacer"></span><button class="btn alt" onclick="closeModal()">Close</button></div><div class="form"><div class="field full"><label>Title</label><input id="et"></div><div class="field"><label>Duration minutes</label><input id="ed" type="number" value="30"></div><div class="field"><label>Pass %</label><input id="ep" type="number" value="35"></div><div class="field full"><label>Description</label><textarea id="exd"></textarea></div></div><button class="btn" style="margin-top:12px" onclick="saveExam()">Publish Exam</button>`)}
async function saveExam(){try{await add('exams',{institute_id:S.profile.institute_id,title:$('et').value.trim(),description:$('exd').value.trim(),duration_minutes:Number($('ed').value),pass_percent:Number($('ep').value),status:'published',created_by:S.profile.id});closeModal();render()}catch(e){toast(e.message)}}
async function learning(){const[c,m,h]=await Promise.all([load('courses'),load('study_materials'),load('homework')]);return `<div class="title"><h2>Teaching & Learning</h2><span class="spacer"></span><button class="btn" onclick="courseForm()">+ Course</button></div><div class="grid g3"><div class="card"><h3>Online Courses</h3><div class="metric">${c.length}</div></div><div class="card"><h3>Study Material</h3><div class="metric">${m.length}</div></div><div class="card"><h3>Homework</h3><div class="metric">${h.length}</div></div></div><div class="list" style="margin-top:12px">${c.map(x=>`<div class="card"><h3>${esc(x.title)}</h3><div class="muted">${esc(x.description||'')} • ${esc(x.status)}</div></div>`).join('')}</div>`}
function courseForm(){modal(`<div class="title"><h2>Create Online Course</h2><span class="spacer"></span><button class="btn alt" onclick="closeModal()">Close</button></div><div class="form"><div class="field full"><label>Title</label><input id="ct"></div><div class="field full"><label>Description</label><textarea id="cd"></textarea></div></div><button class="btn" style="margin-top:12px" onclick="saveCourse()">Save Course</button>`)}
async function saveCourse(){try{await add('courses',{institute_id:S.profile.institute_id,title:$('ct').value.trim(),description:$('cd').value.trim(),status:'published',created_by:S.profile.id});closeModal();render()}catch(e){toast(e.message)}}
async function leads(){const a=await load('leads');return `<div class="title"><h2>Leads / Enquiries</h2><span class="spacer"></span><button class="btn" onclick="leadForm()">+ Enquiry</button></div><div class="grid g4"><div class="card">Total<div class="metric">${a.length}</div></div><div class="card">Open<div class="metric">${a.filter(x=>['new','follow_up','interested'].includes(x.status)).length}</div></div><div class="card">Converted<div class="metric">${a.filter(x=>x.status==='converted').length}</div></div><div class="card">Lost<div class="metric">${a.filter(x=>x.status==='lost').length}</div></div></div><div class="list" style="margin-top:12px">${a.map(x=>`<div class="card"><b>${esc(x.name)}</b><div class="muted">${esc(x.mobile||'')} • ${esc(x.course_interested||'')}</div><span class="badge pending">${esc(x.status)}</span></div>`).join('')}</div>`}
function leadForm(){modal(`<div class="title"><h2>New Enquiry</h2><span class="spacer"></span><button class="btn alt" onclick="closeModal()">Close</button></div><div class="form"><div class="field"><label>Name</label><input id="ln"></div><div class="field"><label>Mobile</label><input id="lm"></div><div class="field"><label>Course</label><input id="lc"></div><div class="field"><label>Source</label><input id="ls" value="walk_in"></div></div><button class="btn" style="margin-top:12px" onclick="saveLead()">Save</button>`)}
async function saveLead(){try{await add('leads',{institute_id:S.profile.institute_id,name:$('ln').value.trim(),mobile:$('lm').value.trim(),course_interested:$('lc').value.trim(),source:$('ls').value.trim(),status:'new'});closeModal();render()}catch(e){toast(e.message)}}
async function accounts(){const[a,e]=await Promise.all([load('institute_accounts'),load('expenses')]);return `<div class="title"><h2>Accounts</h2></div><div class="grid g2"><div class="card"><h3>Accounts</h3><div class="metric">${a.length}</div><div class="muted">Cash, bank and UPI accounts</div></div><div class="card"><h3>Expenses</h3><div class="metric">${money(e.reduce((n,x)=>n+Number(x.amount),0))}</div></div></div><div class="card" style="margin-top:12px"><h3>Accounting reports</h3><div class="muted">Day Book • P&L • Outstanding • Mode-wise Summary • GST summary when enabled.</div></div>`}
async function reports(){const[b,p,e]=await Promise.all([load('fee_bills'),load('fee_payments'),load('expenses')]);return `<div class="title"><h2>Reports & Analytics</h2></div><div class="grid g3"><div class="card"><h3>Outstanding</h3><div class="metric">${money(b.reduce((n,x)=>n+Number(x.net_amount),0)-p.reduce((n,x)=>n+Number(x.amount),0))}</div></div><div class="card"><h3>Collections</h3><div class="metric">${money(p.reduce((n,x)=>n+Number(x.amount),0))}</div></div><div class="card"><h3>Expenses</h3><div class="metric">${money(e.reduce((n,x)=>n+Number(x.amount),0))}</div></div></div><div class="card" style="margin-top:12px"><b>Export</b><p class="muted">PDF/CSV export hooks are ready for the report datasets; production PDF rendering can be connected to the native mobile layer.</p></div>`}
async function access(){const p=await load('user_permissions');return `<div class="title"><h2>Online Access & Permissions</h2></div><div class="card"><h3>Feature permissions</h3><div class="muted">${p.length} permission records</div><div class="empty">Admin can assign features and batches through user_permissions and batch_permissions.</div></div>`}
async function timetable(){const a=await load('timetables');return `<div class="title"><h2>Timetable</h2></div><div class="tablewrap"><table class="table"><tr><th>Day</th><th>Time</th><th>Subject</th><th>Room</th></tr>${a.map(x=>`<tr><td>${esc(x.weekday)}</td><td>${esc(x.start_time)} - ${esc(x.end_time)}</td><td>${esc(x.subject||'')}</td><td>${esc(x.room||'')}</td></tr>`).join('')}</table></div>`}
function profile(){return `<div class="title"><h2>Profile</h2></div><div class="card"><h3>${esc(S.profile.full_name)}</h3><div class="muted">${esc(S.profile.email||S.session.user.email)} • ${esc(S.role)}</div><div style="margin-top:12px">Institute: ${esc(S.institute?.name||'')}</div></div>`}
function settings(){if(S.role!=='owner'&&S.role!=='admin')return '<div class="card">Settings are managed by the institute owner.</div>';return `<div class="title"><h2>Settings</h2></div><div class="grid g2"><div class="card"><h3>Institute</h3><p>${esc(S.institute?.name||'')}</p><p class="muted">GST: ${S.institute?.gst_enabled?'Enabled':'Disabled'}</p></div><div class="card"><h3>Security</h3><p class="muted">App PIN/dark mode and backup controls belong here; cloud email/connectivity configuration is preserved outside this schema.</p></div></div>`}
window.go=go;window.drawer=drawer;window.login=login;window.register=register;window.logout=logout;window.closeModal=closeModal;window.studentForm=studentForm;window.saveStudent=saveStudent;window.filter=filter;window.batchForm=batchForm;window.saveBatch=saveBatch;window.feeForm=feeForm;window.savePayment=savePayment;window.attForm=attForm;window.saveAtt=saveAtt;window.examForm=examForm;window.saveExam=saveExam;window.courseForm=courseForm;window.saveCourse=saveCourse;window.leadForm=leadForm;window.saveLead=saveLead;window.saveSetup=saveSetup;
(async()=>{if(!db){auth();return}const{data:{session}}=await db.auth.getSession();if(!session){auth();return}S.session=session;try{await hydrate();render()}catch(e){toast(e.message);auth()}})();